# Project Rules

1. MUST execute `task lint` and `task test` before finalizing code changes.
2. MUST use English for all code comments and documentation.
3. MUST update instructions in `docs/zerorat/SKILL.md` when adding new features or changing existing ones.